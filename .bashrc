# Bash
alias game-over=exit
alias snakkes=exit
alias ll="ls -al"
alias ls="ls -a --color=auto"

# Git
alias new="start sh -login -i"

# Maven
alias mvnci="mvn clean install"
alias int="mvnci -Pintegrationtest"


